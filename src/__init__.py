"""Quantum Tabla application package.

The project is organised into three packages:

- ``src.core``: application logic (configuration, state, timing, runtime).
- ``src.dashboard``: the Streamlit single-page dashboard.
- ``src.utils``: shared, framework-free helper utilities.
"""
